import { ThemeProvider } from 'react-native-elements'

import Navigation from './Navigation'

const App = () => {
  return (
    <ThemeProvider>
      <Navigation />
    </ThemeProvider>
  )
}

export default App
